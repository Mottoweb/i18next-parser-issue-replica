import styled from 'styled-components';
import { Icons } from '@adnz/ui';

export const Clear = styled(Icons.Close)`
  position: absolute;
  right: 14px;
  top: 0;
  bottom: 0;
  margin: auto;
  cursor: pointer;
`;
